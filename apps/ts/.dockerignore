# Dependencies (reinstalled in Docker build)
node_modules/

# Build output (rebuilt in Docker build)
dist/
out/

# Tests (not needed in production image)
tests/

# TypeScript config (only needed at build stage, already COPYed explicitly)
# tsconfig.json is included — don't ignore it

# Docker files (prevent recursive copy)
Dockerfile
.dockerignore

# Dev & editor files
.env
.env.*
.git/
.gitignore
.vscode/
.idea/

# Bun lock binary (text lockfile is used instead)
bun.lockb

# Coverage & caches
coverage/
*.lcov
.cache/
*.tsbuildinfo

# OS files
.DS_Store
Thumbs.db
